CREATE TABLE produto (
                         id VARCHAR(255) NOT NULL PRIMARY KEY,
                         nome VARCHAR(255),
                         quant_por_caixa INT,
                         quant_cx_fd INT
);

