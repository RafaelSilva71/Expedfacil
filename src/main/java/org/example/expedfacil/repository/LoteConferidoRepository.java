package org.example.expedfacil.repository;

import org.example.expedfacil.model.LoteConferido;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LoteConferidoRepository extends JpaRepository<LoteConferido, Long> {
}
